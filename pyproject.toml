[project]
name = "private-domain-ltv-autogen-demo"
version = "0.1.0"
description = "AutoGen-style multi-agent LTV engine demo for private-domain operations."
requires-python = ">=3.10"
dependencies = []

[tool.ruff]
line-length = 100

